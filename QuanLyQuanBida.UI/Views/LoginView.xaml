<Window x:Class="QuanLyQuanBida.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:QuanLyQuanBida.UI.ViewModels"
        mc:Ignorable="d"
        Title="Đăng nhập - Quản Lý Quán Bida" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Text="QUẢN LÝ QUÁN BIDA" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Grid.Row="0" Margin="0,0,0,20"/>

        <!-- Tên đăng nhập -->
        <StackPanel Grid.Row="1">
            <Label Content="Tên đăng nhập:"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" FontSize="14"/>
        </StackPanel>

        <!-- Mật khẩu -->
        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <Label Content="Mật khẩu:"/>
            <PasswordBox x:Name="PasswordBox" 
                         Height="30" FontSize="14"
                         PasswordChanged="PasswordBox_PasswordChanged"/>
        </StackPanel>

        <!-- Lưu thông tin đăng nhập -->
        <CheckBox Grid.Row="3" Content="Ghi nhớ đăng nhập" 
                  IsChecked="{Binding IsRememberMe}" 
                  Margin="0,10,0,0"/>

        <!-- Hiển thị lỗi -->
        <TextBlock Grid.Row="4" 
                   Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top"
                   MinHeight="20" 
                   TextWrapping="Wrap"/>

        <!-- Nút đăng nhập -->
        <Button Grid.Row="5" 
                Content="Đăng nhập" 
                Height="35" Width="120" 
                Command="{Binding LoginCommand}"
                HorizontalAlignment="Center"
                Margin="0,10,0,0"/>
    </Grid>
</Window>