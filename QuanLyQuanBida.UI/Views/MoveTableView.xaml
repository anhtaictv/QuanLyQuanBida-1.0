<Window x:Class="QuanLyQuanBida.UI.Views.MoveTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyQuanBida.UI.Views"
        mc:Ignorable="d"
        Title="Chuyển Bàn" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Margin="0,0,0,5">
                <Run Text="Chuyển phiên từ bàn:"/>
                <Run Text="{Binding CurrentTableName}" FontWeight="Bold"/>
            </TextBlock>

            <Label Content="Sang bàn trống:" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding FreeTables}"
                      SelectedItem="{Binding SelectedTargetTable}"
                      DisplayMemberPath="Name"
                      Height="30"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Xác nhận Chuyển" Width="120" Margin="0,0,10,0" 
                        Command="{Binding MoveTableCommand}" IsDefault="True"/>
                <Button Content="Hủy" Width="80" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>