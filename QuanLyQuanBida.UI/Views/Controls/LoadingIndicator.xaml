<UserControl x:Class="QuanLyQuanBida.UI.Views.Controls.LoadingIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">
    <Grid Background="#80000000">
        <Grid Width="50" Height="50">
            <Border CornerRadius="25" Background="White" Opacity="0.2"/>
            <Border CornerRadius="25" Background="White" Opacity="0.6">
                <Border.RenderTransform>
                    <RotateTransform x:Name="SpinnerRotate" Angle="0" CenterX="25" CenterY="25"/>
                </Border.RenderTransform>
                <Border.Clip>
                    <EllipseGeometry RadiusX="25" RadiusY="25" Center="25,25"/>
                </Border.Clip>
                <Border CornerRadius="25" Background="Transparent" Width="45" Height="45" Margin="0,0,0,45"/>
            </Border>
            <Border CornerRadius="25" Background="White" Opacity="0.8">
                <Border.RenderTransform>
                    <RotateTransform x:Name="SpinnerRotate2" Angle="0" CenterX="25" CenterY="25"/>
                </Border.RenderTransform>
                <Border.Clip>
                    <EllipseGeometry RadiusX="25" RadiusY="25" Center="25,25"/>
                </Border.Clip>
                <Border CornerRadius="25" Background="Transparent" Width="45" Height="45" Margin="0,0,0,45"/>
            </Border>
        </Grid>
        <TextBlock Text="Đang tải..." Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20"/>
    </Grid>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard RepeatBehavior="Forever">
                    <DoubleAnimation Storyboard.TargetName="SpinnerRotate" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1"/>
                    <DoubleAnimation Storyboard.TargetName="SpinnerRotate2" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1.5"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
</UserControl>