<Window x:Class="QuanLyQuanBida.UI.Views.AddStockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyQuanBida.UI.Views"
        xmlns:viewmodels="clr-namespace:QuanLyQuanBida.UI.ViewModels"
        mc:Ignorable="d"
        Title="Nhập hàng" Height="300" Width="500"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <viewmodels:AddStockViewModel/>
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Label Content="Sản phẩm:"/>
            <ComboBox x:Name="ProductComboBox" Height="25" DisplayMemberPath="Name" 
                      SelectedValuePath="Id" SelectedValue="{Binding SelectedProductId}"
                      ItemsSource="{Binding Products}" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Label Content="Số lượng:"/>
            <TextBox Height="25" Text="{Binding Quantity}" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Label Content="Ghi chú:"/>
            <TextBox Height="25" Text="{Binding Reference}" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Lưu" Width="80" Margin="0,0,5,0" Command="{Binding SaveCommand}"/>
            <Button Content="Hủy" Width="80" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>