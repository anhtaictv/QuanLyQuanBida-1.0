<Window x:Class="QuanLyQuanBida.UI.Views.BatchCreateTableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyQuanBida.UI.Views"
        mc:Ignorable="d"
        Title="Tạo Bàn Hàng Loạt" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20" DataContext="{Binding Form}">
        <StackPanel>
            <Label Content="Tiền tố Mã (Vd: B, VIP):"/>
            <TextBox Text="{Binding Prefix}" Height="25" Margin="0,0,0,10"/>

            <Label Content="Tiền tố Tên (Vd: Bàn, VIP):"/>
            <TextBox Text="{Binding NamePrefix}" Height="25" Margin="0,0,0,10"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <Label Content="Số bắt đầu:"/>
                    <TextBox Text="{Binding StartNumber}" Height="25"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="5,0,5,0">
                    <Label Content="Số kết thúc:"/>
                    <TextBox Text="{Binding EndNumber}" Height="25"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="5,0,0,0">
                    <Label Content="Số đệm (Vd: 2 -> 01):"/>
                    <TextBox Text="{Binding Padding}" Height="25"/>
                </StackPanel>
            </Grid>

            <Label Content="Khu vực (Vd: Tầng 1):" Margin="0,10,0,0"/>
            <TextBox Text="{Binding Zone}" Height="25" Margin="0,0,0,10"/>

            <Label Content="Số chỗ ngồi:"/>
            <TextBox Text="{Binding Seats}" Height="25" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Tạo" Width="100" Margin="0,0,10,0" 
                        Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="Hủy" Width="80" IsCancel="True"
                        Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>