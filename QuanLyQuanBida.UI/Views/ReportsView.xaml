<Window x:Class="QuanLyQuanBida.UI.Views.ReportsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyQuanBida.UI.Views"
        xmlns:viewmodels="clr-namespace:QuanLyQuanBida.UI.ViewModels"
        mc:Ignorable="d"
        Title="Báo cáo và Thống kê" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <viewmodels:ReportsViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Report Type Selection -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Loại báo cáo:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="ReportTypeComboBox" Width="200" Height="25" 
                      SelectedIndex="0" SelectedValue="{Binding SelectedReportType}">
                <ComboBoxItem Content="Doanh thu theo ngày"/>
                <ComboBoxItem Content="Doanh thu theo bàn"/>
                <ComboBoxItem Content="Doanh thu theo sản phẩm"/>
                <ComboBoxItem Content="Công nợ khách hàng"/>
                <ComboBoxItem Content="Tồn kho"/>
            </ComboBox>
        </StackPanel>

        <!-- Date Range Selection -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Từ ngày:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker Width="120" Height="25" SelectedDate="{Binding StartDate}"/>

            <TextBlock Text="Đến ngày:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <DatePicker Width="120" Height="25" SelectedDate="{Binding EndDate}"/>

            <Button Content="Xem báo cáo" Width="100" Margin="20,0,0,0" Command="{Binding GenerateReportCommand}"/>
            <Button Content="Xuất Excel" Width="100" Margin="10,0,0,0" Command="{Binding ExportToExcelCommand}"/>
            <Button Content="In" Width="80" Margin="10,0,0,0" Command="{Binding PrintCommand}"/>
        </StackPanel>

        <!-- Report Content -->
        <Grid Grid.Row="2">
            <TabControl>
                <TabItem Header="Báo cáo">
                    <DataGrid x:Name="ReportDataGrid" AutoGenerateColumns="False" 
                              IsReadOnly="True" ItemsSource="{Binding ReportData}">
                        <DataGrid.Columns>
                            <!-- Columns will be dynamically generated based on report type -->
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="Biểu đồ">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="10">
                            <TextBlock Text="Biểu đồ doanh thu" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                            <!-- Chart control will be added here -->
                            <Border Background="LightGray" Height="300"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10">
                            <TextBlock Text="Biểu đồ sản phẩm bán chạy" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                            <!-- Chart control will be added here -->
                            <Border Background="LightGray" Height="300"/>
                        </StackPanel>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>