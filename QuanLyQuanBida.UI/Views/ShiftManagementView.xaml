<Window x:Class="QuanLyQuanBida.UI.Views.ShiftManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyQuanBida.UI.Views"
        xmlns:viewmodels="clr-namespace:QuanLyQuanBida.UI.ViewModels"
        mc:Ignorable="d"
        Title="Quản lý Ca làm việc" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <viewmodels:ShiftManagementViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Mở ca" Width="100" Command="{Binding OpenShiftCommand}"/>
            <Button Content="Đóng ca" Width="100" Margin="10,0,0,0" Command="{Binding CloseShiftCommand}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Từ ngày:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker Width="120" Height="25" SelectedDate="{Binding StartDate}"/>

            <TextBlock Text="Đến ngày:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <DatePicker Width="120" Height="25" SelectedDate="{Binding EndDate}"/>

            <Button Content="Xem lịch sử" Width="100" Margin="20,0,0,0" Command="{Binding LoadShiftHistoryCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Shifts}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nhân viên" Binding="{Binding User.FullName}" Width="150"/>
                <DataGridTextColumn Header="Bắt đầu" Binding="{Binding StartAt, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Kết thúc" Binding="{Binding EndAt, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Tiền mặt đầu ca" Binding="{Binding OpeningCash, StringFormat='{}{0:N0}'}" Width="120"/>
                <DataGridTextColumn Header="Tiền mặt cuối ca" Binding="{Binding ClosingCash, StringFormat='{}{0:N0}'}" Width="120"/>
                <DataGridTextColumn Header="Chênh lệch" Binding="{Binding Difference, StringFormat='{}{0:N0}'}" Width="120"/>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>